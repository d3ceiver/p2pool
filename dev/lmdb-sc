apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: slave3-data-volume
  namespace: p2pool
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
  storageClassName: monero-data-pv
